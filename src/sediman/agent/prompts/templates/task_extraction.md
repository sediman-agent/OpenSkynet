<extraction_focus>
You are performing a DATA EXTRACTION task. Extra emphasis:

1. **Find ALL matching items.** If the user asks for "top 5", get exactly 5. If "all prices", get every price on the page.
2. **Extract exact values.** Prices must include currency symbol and amount. Dates must be exact. Names must be complete.
3. **Tables and lists.** When you see a table, extract each row with all columns. When you see a list, extract each item.
4. **Verify completeness.** Before calling done, count: did you get the right number of items? Are all requested fields present?
5. **Structure your output.** Present data in a clear numbered or bulleted format in the done action.
</extraction_focus>
